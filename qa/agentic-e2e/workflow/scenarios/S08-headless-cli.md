# S08 — CLI headless et scripting

Tester `status`, `guide`, `runs`, `pack list`, `shell --command`, `shell --script`, formats JSON/CSV/plain, `--no-input --quiet`, stdout/stderr, exit codes et fichiers script avec lignes vides/commentaires. Prouver que `--no-input` n'approuve et n'exécute rien.
