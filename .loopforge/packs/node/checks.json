{
  "version": 1,
  "checks": [
    {
      "name": "git-diff-check",
      "command": ["git", "diff", "--check"],
      "timeout_seconds": 60
    },
    {
      "name": "package-json-valid",
      "command": [
        "{python}",
        "-c",
        "import json, pathlib; p = pathlib.Path('package.json'); json.loads(p.read_text(encoding='utf-8')) if p.exists() else None"
      ],
      "timeout_seconds": 30
    }
  ]
}
