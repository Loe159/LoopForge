# Node Pack

Use this pack for Node.js, JavaScript, and TypeScript projects.

Prefer the package manager and scripts already present in `package.json`.
Treat dependency and lockfile edits as higher-risk changes that need clear
review evidence.
