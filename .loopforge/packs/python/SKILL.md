# Python Pack

Use this pack for Python projects.

Prefer small changes, keep package metadata and dependency edits visible, and
use local deterministic checks before calling work complete. When tests exist,
prefer the project's configured runner; otherwise use syntax and import-level
checks that do not reach the network.
