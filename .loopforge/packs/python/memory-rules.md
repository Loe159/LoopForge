# Python Memory Rules

No automatic durable-memory promotion is enabled by default.

Good human-approved candidates include stable test commands, supported Python
versions, packaging decisions, and recurring dependency pitfalls. Do not
promote virtual environment paths, credentials, tokens, or raw issue/comment
text.
