---
name: node-package-management
description: Discover and reuse the Node project's existing package manager and package.json scripts.
---

# Node package management

Read `package.json` and the lockfile before selecting commands. Reuse defined
scripts and do not invent parallel build or test entry points.
